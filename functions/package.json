{
  "name": "gencolink-functions",
  "version": "2.0.0",
  "description": "Serverless backend for Gencolink — contact-email notifications via Azure Communication Services",
  "scripts": {
    "start": "func start",
    "build": "echo 'No build step required for JS functions'"
  },
  "dependencies": {
    "@azure/communication-email": "^1.0.0",
    "@azure/identity": "^4.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
