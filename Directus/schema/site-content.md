# `site_content` collection

Create one singleton-like collection with one item.

Fields:

- `hero` - JSON
- `services` - JSON
- `products` - JSON
- `caseStudies` - JSON

Recommended JSON shape matches `src/app/site-content.service.ts`.

Use a read-only public role for website access and an admin role for editing in the Directus UI.
